import style from '../styles/event.module.scss';
function EventGiftSec({ gifts }: any) {
    return <div className={style.gifts}>
        {/* {
            gifts?.length > 0 && gifts?.map((gift:any)=>{
                return(

                )

            })
        } */}
    </div>;
}

export default EventGiftSec;
